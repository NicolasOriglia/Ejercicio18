const nombre = 'Nicolas';
const apellido = 'Origlia';

const datos = {
    nombre,
    apellido
};

console.log(datos);

//localStorage.setItem('datosPersonales', JSON.stringify(datos));

//sessionStorage.setItem('datosPersonales', JSON.stringify(datos));

//document.cookie = `datosPersonales=${JSON.stringify(datos)}`;